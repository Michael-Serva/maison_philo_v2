{% extends "dashboard/layout.html.twig" %}
{% block title %}
    {% trans %}Home |{% endtrans %}{{ parent() }}
{% endblock %}
{% block body %}
    <h1 class"font-tangerine">
        {% trans %}Welcome to{% endtrans %}
        {{webSiteName}}
        {% if app.user.pseudo is defined %}
            "{{app.user.pseudo}}"
        {% endif %}
    </h1>
    {% for message in app.flashes('dashboard') %}
        {% include "component/molecules/alertDesign.html.twig" with {
            congrats : 'Well Done! '
        } %}
    {% endfor %}
    <div class="row mb-1 justify-content-center">

        <span class="side col-lg-12 text-center mb-2">{% trans %}The Aging : a coming challenge for Africa{% endtrans %}</span>
        <div class="mb-1 col-12 col-sm-12 col-md-12 col-lg-6 col-xlg-7 ">
            <img class="img-fluid" src="{{ asset('images/couple_agees2.jpg') }}" data-aos="fade-down-right" data-aos-duration="2000">
        </div>
        {% include "component/atoms/presentation.html.twig" with {
            presentationContents : presentationContents
        } %}
    </div>
    <table class="table table-success w-25">
        <thead>
            <h1>{% trans %}Covid case in Ivory Coast{% endtrans %}</h1>
        </thead>
        <tbody>
            <tr>
                <th scope="col">{% trans %}Covid case{% endtrans %}</th>
                <td scope="col">{{datas.confirmed|format_number({grouping_size:3})}}</td>
            </tr>
            <tr>
                <th scope="col">{% trans %}Death{% endtrans %}</th>
                <td scope="col">{{datas.deaths|format_number({grouping_size:3})}}</td>
            </tr>
        </tbody>
    </table>


{% endblock %}
