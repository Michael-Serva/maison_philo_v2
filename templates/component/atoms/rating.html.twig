<span>
    {% for i in 1..5 %}
        {% if i <= comment.rating %}
            <i class="fas fa-star bg-success p-1"></i>
        {% else %}
            <i class="far fa-star"></i>
        {% endif %}
    {% endfor %}
</span>
